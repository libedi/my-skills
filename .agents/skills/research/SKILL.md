---
name: research
description: Research substantive questions through independent investigation axes, source-backed claims, counterarguments, and explicit uncertainty. Use for multi-source fact finding, causal analysis, alternatives, and technical or specification reviews; skip the harness for simple lookups, summaries, translations, and edits.
---

# Research

Use this skill for research, not as authorization to edit source material, publish, contact others, or change an external system. The [integrated design](../../../docs/research-harness-design/research-harness-design.md) explains the rationale. The references below are the execution contract.

## Host Agent

1. Preserve the user's original request verbatim. Make an Intake Packet with confirmed user decisions, hard constraints, source locations, available tools, and limits. Label earlier facts, user statements, and assumptions separately. Exclude tentative solutions, agent recommendations, and the whole prior conversation.
2. Confirm that a fresh-context subagent can start and that custom agent `research_orchestrator` is loaded with its configured model and reasoning effort. The v1 profile is `gpt-6-astra` at `high`. Use `fork_turns="none"` or an equivalent fresh start. A TOML file's presence does not prove that the running agent has those settings or isolated context. If either requirement cannot be verified, do not claim to run the Harness or silently substitute a weaker model. Explain the capability gap and resolve the execution path with the user.
3. Start one Research Orchestrator for the request. Pass only the Intake Packet and the location and version of this skill and its relevant references. Do not assume the Host's loaded skill text is inherited. Keep user decisions, permission boundaries, and final delivery with the Host.
4. Check the first plan against the original request for missing questions or outcomes, changed hard constraints, and unwarranted scope. Return source-linked discrepancies only, without preferred axes, tentative conclusions, or conversation history. The Orchestrator owns axis quality. If [axis design](references/axis-design.md) requires independent plan review, directly spawn a fresh-context general agent without a custom profile. Pass only the original request, confirmed constraints, plan, and common criteria; forward its unchanged result. Preserve the first plan and revisions. Release investigation only after the current plan passes the request check and required review is resolved. Release is a workflow check, not user approval. Use a follow-up task to resume an idle Orchestrator on the same ID.
5. Compare the final report and validation record with the original request. Deliver the result with material evidence, unresolved questions, status, and limitations. Send substantive corrections back to the Orchestrator for reconciliation; do not quietly rewrite findings.

A short self-contained fact lookup does not need the Harness. A substantive research request without a fresh Orchestrator is a capability failure, not an independent multi-agent study.

## Research Orchestrator

Read [agent contracts](references/agent-contracts.md), [axis design](references/axis-design.md), and [evidence and evaluation](references/evidence-and-evaluation.md). Read [complex requests](references/complex-requests.md) when the request has distinct subproblems or dependent deliverables. Use [JSON contracts](references/json-contracts.md) for machine-readable records and the validator. Start from the [Brief template](assets/research-brief.json) and [report template](assets/research-report.md) where helpful. Run `scripts/validate.py` from this skill directory, or call it by its full path from another working directory.

1. Classify purpose, hard constraints, preferences, hypotheses, and required outcomes. Build a Brief from the Intake Packet without promoting a hypothesis or recommendation to fact. For complex requests map every parent question and required outcome to a Subproblem; keep one Orchestrator and execute Research Units sequentially.
2. Design distinct necessary initial axes without a fixed three-axis cap. Record contribution, differentiation, evidence strategy, and independent-start rationale; use one when splitting adds no value. Map every required question. Return the first plan and review triggers to the Host before investigation, then end this turn awaiting a follow-up task. Reconcile feedback without importing Host hypotheses. Run `scripts/validate.py plan --state <state.json>`; structural success does not establish axis quality.
3. Own one ledger. Freeze the N initial axis IDs after plan release. Round 1 allows those N tasks; Round 2 allows at most 2 supplementary tasks, including at most 1 Extra Explorer, 1 Critic, and 1 retry. The Unit investigation ceiling is N + 2. Validate each reservation with `scripts/validate.py dispatch --state <state.json> --ledger <ledger.json> --unit <unit-id>`. One existing-evidence-only Status Check is separate; complex requests may use at most 2 final consistency tasks. Count dispatched failures, cancellations, and same-agent follow-ups; release only pre-dispatch cancellations with a reason.
4. Delegate one level to Explorer or Critic with only the Unit Brief, assignment, common criteria, source-access limits, and output contract. Initial Explorers do not see one another's findings. Use the `research_explorer` (`gpt-6-sol`, `medium`) and `research_critic` (`gpt-6-sol`, `high`) project profiles when they can be selected and their applied settings verified. Otherwise set model and reasoning explicitly for each role and record the actual values; do not accidentally inherit the Orchestrator's costly model. If nested delegation is unavailable, give the Host an exact sibling assignment and receive its unchanged result; keep the ledger and research judgment yourself. There is no fixed three-role cap: Host and Orchestrator are excluded from investigation-role counts but consume shared runtime capacity. Keep Unit execution sequential and confirm capacity for the whole initial cohort before launch. Do not trim required axes or launch only part to fit slots. Disclose insufficient capacity and resolve execution or scope through the Host; a recorded capacity check is not an atomic reservation.
5. Give each initial Explorer at most 30 research-tool calls; same-axis verification, retry, Extra Explorer, and Critic each have at most 10 and consume a supplementary task. Resume never resets usage. Read [evidence and evaluation](references/evidence-and-evaluation.md) for counting and stopping. Collect all initial results before evaluation; record usage and phase timings. Copy each result's usage to its work item and validate with `scripts/validate.py result --state <state.json> --result <result.json> --ledger <ledger.json>`. Read decisive full evidence and distinguish facts, inferences, proposals, counterevidence, and unknowns. Reuse the existing Explorer for an existing-axis gap, Extra Explorer for a new axis, and Critic for reasoning objections. Stop when sufficient; do not hide a critical gap as complete.
6. For `complete` require every critical question answered, required outcomes mapped, no unresolved blocking objection, and no stale essential complex result. Otherwise distinguish `partial` from `needs_user_decision` by cause. Consider one independent, read-only Status Check if proposing `complete` without a Critic. Run `scripts/validate.py report --state <state.json> --ledger <ledger.json>` and record any unrun check.
7. Return a decision-ready report plus plan, ledger, and validation summaries to the Host. Include source locations, applicability, counterevidence, unresolved questions, and the actual exit reason. Never claim that unverified context isolation, model settings, deadlines, or semantic judgments were mechanically enforced.

Use `research/<run-id>/state.json`, `ledger.json`, `results/`, and optionally `report.md` when writable and useful. Avoid unnecessary copies of nonpublic material. If safe file storage or Python is unavailable, carry equivalent structured records inline and mark affected checks unrun. Never present an unrun check as passed.
